# Prometheus scrape configuration for local Go microservices.
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'go-microservices'
    metrics_path: '/metrics'
    static_configs:
      - targets:
          - 'localhost:8080'
          - 'localhost:8081'
