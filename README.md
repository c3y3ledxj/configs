# configs

Personal configuration files and setup notes for my dev environment.

## Contents

- `git/` — global gitignore and aliases
- `vim/` — vimrc and plugin list
- `scripts/` — small helper scripts

## Usage

Clone and symlink what you need:

```bash
ln -s ~/configs/git/gitignore ~/.gitignore
```

Feel free to adapt.
